# Legacy notebook

`CODE_CLEAN.ipynb` is the original monolithic notebook used for the MVA project
submission (Feb 2025). The code has since been split into a proper Python
package under [`src/sb/`](../src/sb/) and CLI entry points under
[`scripts/`](../scripts/). The notebook is kept here purely as a historical
reference; do not run it directly. Use the package and scripts instead.
